# Lock Contention Scenario

## Objective
Simulate multiple transactions trying to update the same "hot" row (e.g., inventory count for a popular item).

## Setup (Planned)
- Concurrent requests updating `Products` table.
- Monitor `Lock Wait Time` in SQL Server.

## Status
Pending Implementation.
