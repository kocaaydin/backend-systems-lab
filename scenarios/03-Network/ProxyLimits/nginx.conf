events {}

http {
    limit_req_zone $binary_remote_addr zone=mylimit:10m rate=50r/s;

    server {
        listen 80;

        location / {
            limit_req zone=mylimit burst=10 nodelay;
            proxy_pass http://external-api:80;
        }
    }
}
